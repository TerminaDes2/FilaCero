// En: app/productos/page.tsx
"use client";

import { ProductsPage } from '@/components/pos/products/ProductsPage';

export default function PaginaDeProductos() {
  // Esta función es el componente que se renderizará en la ruta /productos
  return (
    <ProductsPage />
  );
}